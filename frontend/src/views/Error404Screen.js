/**
 * screen view that invokes a
 * function that renders a 404 response 
 * if a route entered to parse for is not found.
 */
const Error404Screen = {
    render: () => {
        return `<div>Page not found!</div>`;
    },
};
export default Error404Screen;