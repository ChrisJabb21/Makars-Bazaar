{
  "name": "frontend",
  "version": "1.0.0",
  "description": "A clone of amazon for an ecommerce app.",
  "main": "index.js",
  "scripts": {
    "start": "webpack serve --watch-content-base --open --port 9002",
    "build": "webpack --mode=production --output-path ./main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Jabbour",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.14.0",
    "webpack-cli": "^4.3.1",
    "webpack-dev-server": "^3.11.2"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
