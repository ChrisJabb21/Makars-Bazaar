{
  "name": "frontend",
  "version": "1.0.0",
  "description": "A clone of amazon for an ecommerce app.",
  "main": "index.js",
  "scripts": {
    "start": "live-server src --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Jabbour",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}